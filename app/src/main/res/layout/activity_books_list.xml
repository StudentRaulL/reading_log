<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:paddingTop="24dp"
    android:gravity="center"
    tools:ignore="ExtraText">

    <!-- Buton pentru adăugarea cărților -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start"
        android:weightSum="2"
        android:layout_marginBottom="16dp"> <!-- Crește spațiul între butoane și RecyclerView -->

        <Button
            android:id="@+id/addBookButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Add Book"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/loadBooksButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Load Books"/>
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="16dp"/>

    <!-- Spinner pentru sortare și filtrare -->
    <Spinner
        android:id="@+id/filterSortSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:padding="10dp"
        android:background="@drawable/custom_spinner_background"/>

    <!-- Câmp de filtrare - Gen -->
    <EditText
        android:id="@+id/genreFilterInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter genre to filter"
        android:inputType="text"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="@drawable/custom_edittext_background"
        android:textColor="#333333"
        android:textColorHint="#777777"
        android:textSize="16sp"/>

    <!-- Câmp de filtrare - Status -->
    <EditText
        android:id="@+id/statusFilterInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter status (e.g., Available, Read)"
        android:inputType="text"
        android:layout_marginTop="8dp"
        android:padding="12dp"
        android:background="@drawable/custom_edittext_background"
        android:textColor="#333333"
        android:textColorHint="#777777"
        android:textSize="16sp"/>

    <!-- Buton pentru aplicarea filtrului -->
    <Button
        android:id="@+id/applyFilterButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Apply Filter"
        android:layout_marginTop="16dp"/>


    <!-- RecyclerView pentru afișarea cărților -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewBooks"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:maxHeight="300dp"
        android:layout_marginTop="32dp"/>

</LinearLayout>
